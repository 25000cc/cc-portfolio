import { inter } from './fonts'

const Footer = () => {
  return (
    <footer className={`${inter.className} bg-white flex justify-center`}>
        <span className='py-7 font-bold text-gray-900'>@25000cc.com</span>
    </footer>
  );
};

export default Footer;